[package]
name = "hacl"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
krml = { path = "../krml" }
fstar = { path = "../fstar" }
lowfstar = { path = "../lowfstar" }

[lib]
name = "hacl"
path = "lib.rs"
