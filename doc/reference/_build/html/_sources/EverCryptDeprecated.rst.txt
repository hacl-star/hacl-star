Deprecated block cipher API (``EverCrypt_Cipher.h``)
----------------------------------------------------

We decided to only offer an API for CTR mode rather than exposing the raw block
cipher function. Clients should used ``EverCrypt_CTR.h`` instead.

